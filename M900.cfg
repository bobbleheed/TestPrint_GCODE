##This GCODE macro was written by Ellis on the VoronDesign discord.

[gcode_macro _M900]
gcode:
    #Marlin Linear Advance Copatibility 
    # Parameters
    {% set k = params.K|default(0)|float %}
    
    SET_PRESSURE_ADVANCE ADVANCE={k}
